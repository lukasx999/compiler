defun foo(a'void) -> void { ret; }


defun max(one'int, two'int) -> int {
    if one > two {
        ret one;
    }
    else {
        ret two;
    }
}

defun sum(a'int, b'int) -> int {
    ret a + b;
}

defun main(argc'int, argv'str) -> int {
    ret 0;
}
